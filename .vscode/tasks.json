{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "g++", // or "clang++" for Clang
            "args": [
                "-std=c++20",   // Use C++11 standard
                "-o", "main", // Output file
                "main.cc" // Your source file(s)
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
